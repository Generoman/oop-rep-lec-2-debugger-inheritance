package inheritance.interfaces;

public interface HasGenres {

    // En metode uten "body", som vil si at den ikke kan kjøres slik den står nå.
    // Denne må implementeres (dvs man må skrive en egen versjon av den) i hver klasse
    // som implementerer dette interfacet
    void showGenres();
}
